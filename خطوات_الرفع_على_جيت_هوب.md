# خطوات رفع سوقي | SOUQI على GitHub

> **الحالة:** المستودع المحلي جاهز بـ **4 التزامات (commits)** على فرع `main`، و**34 ملفًا**، وحجم التاريخ 731 كيلوبايت.
> لم يتم أي رفع بعد، لأن البيئة ما فيهاش بيانات اعتماد (توكن) لحسابك على GitHub.

---

## أولًا: اعمل المستودع على GitHub (من عندك، دقيقة واحدة)

1. افتح [github.com/new](https://github.com/new)
2. الاسم المقترح: **`souqi`** — أو `souqi-prototype`
3. اختر **Private** أو **Public** زي ما تحب
4. ⚠️ **مهم:** لا تُعلّم على «Add a README file» ولا «.gitignore» ولا «license» — سيبه **فاضي تمامًا**
   (لو عملت ملفات هناك، الرفع هيرفض بسبب تعارض التاريخ، ولازم `git pull --rebase` الأول)
5. اضغط **Create repository** وانسخ الرابط، شكله كده: `https://github.com/USERNAME/souqi.git`

---

## الطريقة الأولى: أرفع أنا من هنا مباشرة (الأسرع)

ابعتلي في الشات:

```
رابط المستودع: https://github.com/USERNAME/souqi.git
توكن الرفع: ghp_xxxxxxxxxxxxxxxx
```

وأنا هعمل الرفع في ثوانٍ بالأمر ده من البيئة (الشبكة عندي متصلة بـ GitHub):

```bash
git remote add origin https://USERNAME:TOKEN@github.com/USERNAME/souqi.git
git push -u origin main
git remote remove origin     # نشيل الرابط اللي فيه التوكن فورًا بعد الرفع
```

### كيف تعمل توكن بأقل صلاحية ممكنة (مهم للأمان)

1. افتح [github.com/settings/tokens?type=beta](https://github.com/settings/tokens?type=beta) (Fine-grained tokens)
2. **Token name:** `souqi-push` · **Expiration:** 7 أيام (أو أقل)
3. **Repository access:** Only select repositories → اختر مستودع `souqi` بس
4. **Permissions:** `Contents` = **Read and write** — وبس. مش محتاج أي صلاحية تانية
5. Generate → انسخ التوكن وابعته

> 🔒 **ملاحظتان بصراحة:** (1) التوكن هيظهر في نص المحادثة دي، فالأفضل تحدد له مدة قصيرة و**تلغيه بعد الرفع مباشرة** من نفس الصفحة؛
> (2) ملف `.git/config` مستثنى من حفظ مساحة العمل لأسباب أمنية، فأنا بستخدم التوكن في أمر الرفع نفسه مش بتخزينه على القرص.

---

## الطريقة الثانية: ارفع من جهازك (من غير ما تبعت أي توكن)

في مجلد المشروع فيه ملف **`souqi-repo.bundle`** — ده ملف واحد جواه **كل تاريخ المشروع**. نزّله، وبعدين:

```bash
# 1) استخرج المستودع من الملف
git clone souqi-repo.bundle souqi
cd souqi

# 2) اربطه بمستودعك على GitHub
git remote add origin https://github.com/USERNAME/souqi.git

# 3) ارفع
git push -u origin main
```

**ولو عندك مستودع جاهز وعايز تدمج التاريخ فيه:**

```bash
cd موجود-عندك
git remote add souqi /path/to/souqi-repo.bundle
git fetch souqi
git checkout -b main souqi/main      # أو: git merge souqi/main
git push -u origin main
```

---

## الطريقة الثالثة: من غير git خالص

فيه ملف **`souqi-project.zip`** فيه كل ملفات المشروع جاهزة. فُكّ الضغط، وبعدين من صفحة المستودع الفاضية على GitHub اختار
**uploading an existing file** واسحب الملفات — بس كده الالتزامات هتكون التزامًا واحدًا بدل 4.

---

## إيه اللي هيتـرفع بالظبط (34 ملفًا)

| المجموعة | الملفات |
|---|---|
| النموذج الجاهز | `souqi.html` (483KB — الملف الواحد الكامل) |
| الكود المصدري | `src/` — 12 ملفًا: الأيقونة، البيانات، المحرك، الهيكل، الخرائط، **المحلات الحقيقية**، الصفحات، صفحتان إضافيتان، الأدوات، وحدة التسجيل (2)، المساعد، التهيئة |
| البناء | `build.sh` — دمج + فحص صياغة + حقن + تشغيل كل الاختبارات |
| الاختبارات | `tests/` — 4 ملفات (**157 اختبارًا** فعليًا: 45 رسم + 42 مسارًا + اختبارات التسجيل + 70 اختبارًا للمحلات الحقيقية والأيقونة) |
| الهوية | `هوية_سوقي_الالوان.html` · `souqi-icon.svg` · 7 مقاسات PNG · `tools/make_icon.py` |
| التوثيق | `README.md` · `خطة_جمع_البيانات.md` |

**ملفات مش هتـرفع عن قصد:** `souqi-repo.bundle` و `souqi-project.zip` (أدوات نقل مؤقتة، مش جزء من المشروع).

---

## بعد الرفع: أوامر مفيدة

```bash
git log --oneline                 # مراجعة الالتزامات الأربعة
bash build.sh                     # بناء + اختبار كامل (لازم يطلع كله أخضر)
git status                        # لازم يقول: working tree clean
```

> ✅ **البناء حتمي:** شغّلت `bash build.sh` بعد الالتزامات الأربعة، والنتيجة **صفر تغييرات** في `git status` — يعني أي حد يستنسخ المستودع ويشغّل البناء هيوصل لنفس الملف بالحرف.
