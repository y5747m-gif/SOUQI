# خطوات رفع سوقي | SOUQI على GitHub

> **الحالة:** المستودع المحلي جاهز بـ **4 التزامات (commits)** على فرع `main`، و**34 ملفًا**، وحجم التاريخ 731 كيلوبايت.
> لم يتم أي رفع بعد، لأن البيئة ما فيهاش بيانات اعتماد (توكن) لحسابك على GitHub.

---

# ✅ تم الرفع بنجاح — 19 سبتمبر 2026

| | |
|---|---|
| **المستودع** | [github.com/y5747m-gif/SOUQI](https://github.com/y5747m-gif/SOUQI) (عام) |
| **الفرع** | `main` |
| **آخر التزام** | `a324458` — مطابق بين المحلي و GitHub |
| **المحتوى** | **37 ملفًا** · 1.26 ميجابايت · 8 التزامات |
| **التوكن** | استُخدم في أمر الرفع فقط · غير مخزّن في أي ملف أو في `.git/config` |

**مهم — الأدوار القادمة:** مجلد `.git/config` مستثنى من حفظ مساحة العمل لأسباب أمنية، فلو رجعنا في رسالة جديدة:
- رابط المستودع والهوية هيتفكوا → الحل: `git remote add origin https://github.com/y5747m-gif/SOUQI.git`
- أو ببساطة: `SOUQI_REPO="https://github.com/y5747m-gif/SOUQI.git" SOUQI_TOKEN="توكن جديد" bash tools/push.sh`
- (الالتزامات والملفات نفسها **محفوظة** — اللي بيتأثر بس إعدادات المستودع المحلية)

**لينك مباشر للنموذج (بعد تفعيل Pages):** `https://y5747m-gif.github.io/SOUQI/`
— التفعيل: Settings → Pages → Source: *Deploy from a branch* → Branch: **main** + **/ (root)** → Save (ويشتغل خلال دقيقة).

---

# 🚀 ابدأ من هنا — المستودع لسه مش موجود؟ (خطوة بخطوة، دقيقتان)

## المسار (أ) — أنا أعمل المستودع… وأنت تنسخ وتلصق [الموصى به]

### الخطوة 1: اعمل حساب أو ادخل
افتح [github.com/login](https://github.com/login) — لو مفيش حساب اعمل واحد من [github.com/signup](https://github.com/signup) (الإيميل وكلمة سر واسم مستخدم، وكل حاجة مجانية).

### الخطوة 2: اعمل المستودع
1. افتح الرابط ده بالظبط: **[github.com/new](https://github.com/new)**
2. في خانة **`Repository name`** اكتب: **`souqi`**
3. تحت، اختار **`Private`** (محدش يشوفه غيرك) — أو `Public` لو عايز تشاركه
4. ⚠️ **مهم جدًا:** سيب **كل** الخيارات اللي تحت فاضية — متعلّمش على:
   - ❌ `Add a README file`
   - ❌ `Add .gitignore`
   - ❌ `Choose a license`
5. اضغط الزر الأخضر **`Create repository`**
6. هتظهر لك صفحة فيها لينك شكله كده — **انسخه** بالضغط على أيقونة النسخ 📋 جنبه:
   **`https://github.com/اسمك-على-جيت-هوب/souqi.git`**

### الخطوة 3: اعمل توكن الرفع (المفتاح اللي يسمح لي أرفع)
1. افتح: **[github.com/settings/tokens?type=beta](https://github.com/settings/tokens?type=beta)**
2. اضغط **`Generate new token`** (أو `Generate new fine-grained personal access token`)
3. املأ:
   - **`Token name`**: اكتب `souqi-push`
   - **`Expiration`**: اختار **7 days** (أو 1 day لو عايز أقصى أمان)
   - **`Repository access`**: اختار **`Only select repositories`** ← وبعدين اختار مستودع **`souqi`** من القائمة
4. اضغط **`Repository permissions`** ↓ وشغّل **`Contents`** وحوّلها إلى **`Read and write`**
   (بس كده — مش محتاج أي صلاحية تانية، ولا تخليها `All repositories`)
5. اضغط **`Generate token`** في آخر الصفحة
6. هيظهر لك توكن شكله `github_pat_xxxxxxxx…` — **انسخه فورًا** (مش هيظهر تاني بعد ما تسيب الصفحة)

### الخطوة 4: ابعتلي الاتنين في رسالة واحدة

انسخ القالب ده واملا الفراغات والصقه في الشات:

```
رابط المستودع: https://github.com/________/souqi.git
التوكن: github_pat_________
```

وأنا هعمل الباقي: ربط المستودع + رفع الالتزامات الخمسة + التحقق من نجاح الرفع، وهقولك **الغي التوكن** بعدها مباشرة من [نفس الصفحة](https://github.com/settings/tokens).

---

## المسار (ب) — عايزني أنا أعمل المستودع كمان؟ (أقل خطوات، صلاحية أوسع)

في الخطوة 3 فوق، بدل ما تحدد مستودعًا واحدًا:
- **`Repository access`**: اختار **`All repositories`**
- وفي الصلاحيات شغّل **`Administration`** → **`Read and write`** (بجانب `Contents` = `Read and write`)

وبعدين ابعتلي `اسم المستخدم على جيت هوب + التوكن` بس، وأنا هعمل: إنشاء المستودع `souqi` + رفعه + تأكيد الرابط.
> ⚠️ الصلاحية دي أوسع (تقدر تعمل مستودعات على حسابك)، فالأفضل تلغيها فورًا بعد الرفع — وأنا هفكرك.

---
## أولًا: اعمل المستودع على GitHub (من عندك، دقيقة واحدة)

1. افتح [github.com/new](https://github.com/new)
2. الاسم المقترح: **`souqi`** — أو `souqi-prototype`
3. اختر **Private** أو **Public** زي ما تحب
4. ⚠️ **مهم:** لا تُعلّم على «Add a README file» ولا «.gitignore» ولا «license» — سيبه **فاضي تمامًا**
   (لو عملت ملفات هناك، الرفع هيرفض بسبب تعارض التاريخ، ولازم `git pull --rebase` الأول)
5. اضغط **Create repository** وانسخ الرابط، شكله كده: `https://github.com/USERNAME/souqi.git`

---

## الطريقة الأولى: أرفع أنا من هنا مباشرة (الأسرع)

ابعتلي في الشات:

```
رابط المستودع: https://github.com/USERNAME/souqi.git
توكن الرفع: ghp_xxxxxxxxxxxxxxxx
```

وأنا هعمل الرفع بسكربت جاهز ومجرَّب في المشروع (`tools/push.sh`) — جربته فعلًا على مستودع تجريبي ورفع الـ 5 التزامات و35 ملفًا بنجاح:

```bash
SOUQI_REPO="https://github.com/USERNAME/souqi.git" SOUQI_TOKEN="xxx" bash tools/push.sh
# ومع --create  يعمل المستودع تلقائيًا (لو التوكن عنده صلاحية Administration)
```

السكربت: يستخدم التوكن في أمر الرفع فقط · ما يخزّنهش على القرص · يخفي أي توكن من رسائل الخطأ · يرجع الرابط نضيفًا بعد الرفع.

### كيف تعمل توكن بأقل صلاحية ممكنة (مهم للأمان)

1. افتح [github.com/settings/tokens?type=beta](https://github.com/settings/tokens?type=beta) (Fine-grained tokens)
2. **Token name:** `souqi-push` · **Expiration:** 7 أيام (أو أقل)
3. **Repository access:** Only select repositories → اختر مستودع `souqi` بس
4. **Permissions:** `Contents` = **Read and write** — وبس. مش محتاج أي صلاحية تانية
5. Generate → انسخ التوكن وابعته

> 🔒 **ملاحظتان بصراحة:** (1) التوكن هيظهر في نص المحادثة دي، فالأفضل تحدد له مدة قصيرة و**تلغيه بعد الرفع مباشرة** من نفس الصفحة؛
> (2) ملف `.git/config` مستثنى من حفظ مساحة العمل لأسباب أمنية، فأنا بستخدم التوكن في أمر الرفع نفسه مش بتخزينه على القرص.

---

## الطريقة الثانية: ارفع من جهازك (من غير ما تبعت أي توكن)

في مجلد المشروع فيه ملف **`souqi-repo.bundle`** — ده ملف واحد جواه **كل تاريخ المشروع**. نزّله، وبعدين:

```bash
# 1) استخرج المستودع من الملف
git clone souqi-repo.bundle souqi
cd souqi

# 2) اربطه بمستودعك على GitHub
git remote add origin https://github.com/USERNAME/souqi.git

# 3) ارفع
git push -u origin main
```

**ولو عندك مستودع جاهز وعايز تدمج التاريخ فيه:**

```bash
cd موجود-عندك
git remote add souqi /path/to/souqi-repo.bundle
git fetch souqi
git checkout -b main souqi/main      # أو: git merge souqi/main
git push -u origin main
```

---

## الطريقة الثالثة: من غير git خالص

فيه ملف **`souqi-project.zip`** فيه كل ملفات المشروع جاهزة. فُكّ الضغط، وبعدين من صفحة المستودع الفاضية على GitHub اختار
**uploading an existing file** واسحب الملفات — بس كده الالتزامات هتكون التزامًا واحدًا بدل 4.

---

## إيه اللي هيتـرفع بالظبط (34 ملفًا)

| المجموعة | الملفات |
|---|---|
| النموذج الجاهز | `souqi.html` (483KB — الملف الواحد الكامل) |
| الكود المصدري | `src/` — 12 ملفًا: الأيقونة، البيانات، المحرك، الهيكل، الخرائط، **المحلات الحقيقية**، الصفحات، صفحتان إضافيتان، الأدوات، وحدة التسجيل (2)، المساعد، التهيئة |
| البناء | `build.sh` — دمج + فحص صياغة + حقن + تشغيل كل الاختبارات |
| الاختبارات | `tests/` — 4 ملفات (**157 اختبارًا** فعليًا: 45 رسم + 42 مسارًا + اختبارات التسجيل + 70 اختبارًا للمحلات الحقيقية والأيقونة) |
| الهوية | `هوية_سوقي_الالوان.html` · `souqi-icon.svg` · 7 مقاسات PNG · `tools/make_icon.py` |
| التوثيق | `README.md` · `خطة_جمع_البيانات.md` |

**ملفات مش هتـرفع عن قصد:** `souqi-repo.bundle` و `souqi-project.zip` (أدوات نقل مؤقتة، مش جزء من المشروع).

---

## بعد الرفع: أوامر مفيدة

```bash
git log --oneline                 # مراجعة الالتزامات الأربعة
bash build.sh                     # بناء + اختبار كامل (لازم يطلع كله أخضر)
git status                        # لازم يقول: working tree clean
```

> ✅ **البناء حتمي:** شغّلت `bash build.sh` بعد الالتزامات الأربعة، والنتيجة **صفر تغييرات** في `git status` — يعني أي حد يستنسخ المستودع ويشغّل البناء هيوصل لنفس الملف بالحرف.
