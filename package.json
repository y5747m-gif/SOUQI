{
  "name": "souqi",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "build": "bash build.sh",
    "test": "bash build.sh"
  }
}
